import React from "react";
import {CustomButton} from "@/app/components";

const Page = () => {
    return (
            <CustomButton label="fuc" backgroundColor="color-red"/>
    );
};

export default Page;